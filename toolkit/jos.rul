format:
	/* 此?描述?接策略 */
	code(align:1, logic:0x24,      file:0x24);
	data(align:4, logic:stack_end, file:code_end);

file:
	/* 此?描述不能在命令行中写入的.obj文件和.lib文件 */
	/* 注意：此部分可以写成全路径 */
	/* 例：  c:/osask/gg00libc.lib;  */
	./z_tools/haribote/harilibc.lib;
	./z_tools/haribote/golibc.lib;

label:
	/* 指定必??接的?? */
	/* 指定入口点 */
	_Main;

	/* 以上3段的?序不得更改！*/
